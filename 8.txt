1.指针类型

    （1）决定了指针在被解引用的时候，访问的权限
	例如：
	  int a = 0x11223344;
	  char * pa = &a;
	  *pa = 0;
	  这样只会在内存中改变一个字节的内容。
	  
	（2）决定了在指针在移动一步的时候走多大的距离
	
2.指针减指针
（指针减指针的前提是两个指针必须指向同一块空间）
指针减指针得到是指针和指针之间的元素个数（整型）。


3.当对一个结构体中一个字符串进行修改值的时候，需要使用到strcpy（）函数，它的两个参数都是地址。